class Trip {
  constructor(details) {
    this.name = details.name.value;
    this.startDate = details.startDate.value;
    this.endDate = details.endDate.value;
    this.location = details.location.value;
    this.description = details.description.value;
  };

};

// module.exports = Trip;
