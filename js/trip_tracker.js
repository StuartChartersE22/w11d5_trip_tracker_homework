const Trip = require(`../models/Trip.js`)

document.addEventListener('DOMContentLoaded', () => {
  console.log('JavaScript loaded');
});


const detailForm = document.querySelector(`.trip-detail-form`);
